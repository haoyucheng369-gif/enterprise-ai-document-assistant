# RAG

RAG modules will contain retrieval-augmented generation components.

Planned areas:

- `Embeddings`: create embedding vectors for document chunks and questions.
- `VectorSearch`: search vector indexes.
- `Retrieval`: select relevant chunks for a user request.
- `Citations`: map retrieved chunks back to document sources.

